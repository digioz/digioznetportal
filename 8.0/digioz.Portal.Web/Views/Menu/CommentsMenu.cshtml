@model digioz.Portal.Web.Models.ViewModels.CommentsMenuViewModel
 
@if (Model != null && Model.CommentsEnabled == true)
{
    @await Component.InvokeAsync("CommentList", new { referenceId = Model.ReferenceId, referenceType = Model.ReferenceType })


}
