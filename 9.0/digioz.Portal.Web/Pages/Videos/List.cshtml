@page
@model ListModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@addTagHelper *, LazZiya.TagHelpers

@{
    ViewData["Title"] = "Videos";
}

<div class="container my-5">
    <div class="row mb-4">
        <div class="col-md-8">
            <h1>Videos</h1>
        </div>
        @if (User?.Identity?.IsAuthenticated == true)
        {
            <div class="col-md-4 text-end">
                <a asp-page="Add" class="btn btn-primary">Add Video</a>
            </div>
        }
    </div>

    @if (Model.Videos.Any())
    {
        <div class="row">
            @foreach (var video in Model.Videos)
            {
                <div class="col-md-3 col-sm-6 mb-4">
                    <div class="card h-100 shadow-sm @(!video.Visible || !video.Approved ? "border-warning" : "")">
                        <a asp-page="Details" asp-route-id="@video.Id" asp-route-source="list" class="text-decoration-none position-relative">
                            <img src="/img/Videos/Thumb/@video.Thumbnail" 
                                 alt="@video.Description" 
                                 class="card-img-top" 
                                 style="height: 250px; object-fit: cover; @(!video.Visible || !video.Approved ? "opacity: 0.7;" : "")" />
                            
                            @if (!video.Visible || !video.Approved)
                            {
                                <div class="position-absolute top-0 end-0 m-2">
                                    @if (!video.Visible)
                                    {
                                        <span class="badge bg-secondary" title="This video is hidden">
                                            <i class="bi bi-eye-slash"></i> Hidden
                                        </span>
                                    }
                                    @if (!video.Approved)
                                    {
                                        <span class="badge bg-warning text-dark" title="This video is pending approval">
                                            <i class="bi bi-clock-history"></i> Pending
                                        </span>
                                    }
                                </div>
                            }
                        </a>
                        <div class="card-body">
                            <p class="card-text small text-muted">@video.Description</p>
                            <small class="text-muted d-block">
                                Album: @(Model.AlbumNames.TryGetValue(video.AlbumId, out var name) ? name : "-")
                            </small>
                            <small class="text-muted d-block">
                                @video.Timestamp?.ToLocalTime().ToString("yyyy-MM-dd HH:mm")
                            </small>
                            @if (!video.Visible || !video.Approved)
                            {
                                <div class="mt-2">
                                    @if (!video.Visible)
                                    {
                                        <span class="badge bg-secondary small">Hidden</span>
                                    }
                                    @if (!video.Approved)
                                    {
                                        <span class="badge bg-warning text-dark small">Pending Approval</span>
                                    }
                                </div>
                            }
                        </div>
                        <div class="card-footer bg-transparent">
                            <a asp-page="Details" asp-route-id="@video.Id" asp-route-source="list" class="btn btn-sm btn-outline-primary w-100">View</a>
                        </div>
                    </div>
                </div>
            }
        </div>

        <nav aria-label="Page navigation" class="mt-4">
            <ul class="pagination justify-content-center">
                <paging total-records="@Model.TotalCount"
                        page-no="@Model.PageNumber"
                        page-size="@Model.PageSize"
                        show-prev-next="true"
                        show-first-last="true"
                        query-string-key-page-no="pageNumber"
                        class="pagination mb-0"></paging>
            </ul>
        </nav>

        <div class="text-center text-muted small mt-2">
            Page @Model.PageNumber of @Model.TotalPages (@Model.TotalCount total videos)
        </div>
    }
    else
    {
        <div class="alert alert-info text-center py-5">
            <p>No videos found.</p>
        </div>
    }
</div>